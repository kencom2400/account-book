# docker-compose.yml
services:
  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile.dev
    container_name: account-book-backend
    ports:
      - '3001:3001'
    volumes:
      # ソースコードをマウント（ホットリロード用）
      - ./apps/backend/src:/app/apps/backend/src
      - ./libs:/app/libs
      # node_modulesは除外（コンテナ内のものを使用）
      - /app/node_modules
      - /app/apps/backend/node_modules
    environment:
      - NODE_ENV=development
      - PORT=3001
    env_file:
      - .env
    networks:
      - account-book-network
    command: pnpm dev
    healthcheck:
      test: ['CMD', 'wget', '--spider', '-q', 'http://localhost:3001/']
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile.dev
    container_name: account-book-frontend
    ports:
      - '3000:3000'
    volumes:
      # ソースコードをマウント（ホットリロード用）
      - ./apps/frontend/src:/app/apps/frontend/src
      - ./libs:/app/libs
      # Next.jsの設定ファイル
      - ./apps/frontend/next.config.ts:/app/apps/frontend/next.config.ts
      - ./apps/frontend/tailwind.config.ts:/app/apps/frontend/tailwind.config.ts
      - ./apps/frontend/tsconfig.json:/app/apps/frontend/tsconfig.json
      # node_modulesは除外
      - /app/node_modules
      - /app/apps/frontend/node_modules
    environment:
      - NODE_ENV=development
      - PORT=3000
      - NEXT_PUBLIC_API_URL=http://localhost:3001
    env_file:
      - .env
    networks:
      - account-book-network
    depends_on:
      - backend
    command: pnpm dev

networks:
  account-book-network:
    driver: bridge

# 将来のMySQL追加用（Issue #165 対応時に有効化）
# volumes:
#   mysql-data:

