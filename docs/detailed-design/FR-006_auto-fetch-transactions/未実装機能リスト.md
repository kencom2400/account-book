# FR-006 未実装機能リスト

このドキュメントは、FR-006の初期実装（Issue #28, PR #278）および追加実装（Issue #279, PR #285）で未実装となった機能をリストアップしています。

---

## ✅ 完了済み（Issue #279, PR #285）

### 金融機関API連携（一部完了）

**実装内容**:

- ✅ クレジットカード取引取得（`FetchCreditCardTransactionsUseCase`統合）
- ✅ 証券取引取得（`FetchSecurityTransactionsUseCase`統合）
- ⚠️ 銀行取引は依存関係のため未実装（`FetchBankTransactionsUseCase`が存在しない）

**実装箇所**: `SyncAllTransactionsUseCase.syncOne()`

```typescript
switch (target.institutionType) {
  case 'credit-card': {
    const transactions = await this.fetchCreditCardTransactionsUseCase.execute({
      creditCardId: target.institutionId,
      forceRefresh: _forceFullSync,
      abortSignal: abortController.signal,
    });
    break;
  }
  case 'securities': {
    const transactions = await this.fetchSecurityTransactionsUseCase.execute({
      accountId: target.institutionId,
      forceRefresh: _forceFullSync,
      abortSignal: abortController.signal,
    });
    break;
  }
  case 'bank': {
    // 技術的理由: FetchBankTransactionsUseCaseが未実装
    this.logger.warn(`銀行取引の取得は未実装です`);
    break;
  }
}
```

### 同期キャンセル処理（AbortController導入）

**実装内容**:

- ✅ `AbortController`を使用した実行中の非同期処理の停止
- ✅ `CancellationError`カスタムエラークラスの作成
- ✅ `SyncAllTransactionsUseCase`にAbortController管理機能追加
- ✅ 各UseCaseでAbortSignalサポート
- ✅ `CancelSyncUseCase`との統合

**実装箇所**:

- `src/common/errors/cancellation.error.ts`（新規作成）
- `SyncAllTransactionsUseCase.syncOne()`
- `FetchCreditCardTransactionsUseCase.execute()`
- `FetchSecurityTransactionsUseCase.execute()`
- `CancelSyncUseCase.execute()`

**実装パターン**:

```typescript
// AbortController管理
private syncAbortControllers = new Map<string, AbortController>();

async syncOne(target: SyncTarget) {
  const abortController = new AbortController();
  this.syncAbortControllers.set(syncHistory.id, abortController);

  try {
    // signalを子UseCaseに渡す
    await this.fetchCreditCardTransactionsUseCase.execute({
      creditCardId: target.institutionId,
      abortSignal: abortController.signal,
    });
  } catch (error) {
    if (error instanceof CancellationError) {
      // キャンセル処理
      syncHistory = syncHistory.markAsCancelled();
      return { ...result, status: 'CANCELLED' };
    }
    throw error;
  } finally {
    this.syncAbortControllers.delete(syncHistory.id);
  }
}

cancelSync(syncId: string): boolean {
  const controller = this.syncAbortControllers.get(syncId);
  if (controller) {
    controller.abort();
    return true;
  }
  return false;
}
```

---

## 🔴 Critical: 銀行口座取引の取得（未実装）

### 概要

`SyncAllTransactionsUseCase.syncOne()`メソッドで、銀行口座からのデータ取得ロジックが未実装です。

### 現状

- クレジットカードと証券は実装済み
- 銀行口座のみ未実装
- 技術的理由: `FetchBankTransactionsUseCase`が存在しない

### 実装に必要な機能

- FR-001: 銀行口座連携（未実装）
- `FetchBankTransactionsUseCase`の作成
- `IBankRepository`, `IBankAPIClient`の実装
- `BankModule`の作成

### 対応方針

- FR-001の実装後に対応
- 別Issue/PRで実装予定

---

## 🟡 Medium: 動的スケジュール更新

### 概要

`ScheduledSyncJob.updateSchedule()`メソッドで、実行中のcronジョブのスケジュールを動的に変更する機能が未実装です。

### 現状

- `currentSchedule`プロパティを更新するのみ
- アプリケーション再起動が必要
- `TODO`コメント: `// TODO: 動的スケジュール更新の実装`

### Geminiレビュー指摘

> `SchedulerRegistry`を使用して動的にジョブを再登録する実装を推奨します。もしこのPRで実装しない場合は、メソッド名を`setNextScheduleOnRestart`のように、再起動後に適用されることがわかる名前に変更することを検討してください。

### 実装案

```typescript
import { SchedulerRegistry } from '@nestjs/schedule';
import { CronJob } from 'cron';

updateSchedule(cronExpression: string, timezone?: string): void {
  this.currentSchedule = cronExpression;
  if (timezone) {
    this.timezone = timezone;
  }

  // 既存のジョブを削除
  const jobName = 'scheduled-sync';
  const existingJob = this.schedulerRegistry.getCronJob(jobName);
  if (existingJob) {
    existingJob.stop();
    this.schedulerRegistry.deleteCronJob(jobName);
  }

  // 新しいスケジュールでジョブを再登録
  const job = new CronJob(
    this.currentSchedule,
    () => { void this.handleCron(); },
    null,
    this.enabled,
    this.timezone,
  );
  this.schedulerRegistry.addCronJob(jobName, job);
  job.start();

  this.logger.log(`スケジュールを動的に更新しました: ${cronExpression}`);
}
```

### 対応方針

- 別Issue/PRで実装予定
- または、メソッド名を`setNextScheduleOnRestart()`に変更してドキュメント化

---

## 🟡 Medium: 同期進捗の計算

### 概要

`GetSyncStatusUseCase`で、リアルタイムの進捗情報の計算がモック実装になっています。

### 現状

- 固定値（totalInstitutions: 5, completedInstitutions: 2, percentage: 40）を返す
- `TODO`コメント: `// TODO: 進捗情報の計算（現在はモック）`

### Geminiレビュー指摘

> このままでは、APIは常に固定の進捗(`40%`)を返してしまいます。実際の進捗を計算・取得するロジックを実装する必要があります。

### 実装案

1. **共有状態管理**

   ```typescript
   // sync.module.ts に SharedStateService を追加
   @Injectable()
   export class SyncProgressService {
     private currentProgress = new Map<string, SyncProgress>();

     setProgress(syncId: string, progress: SyncProgress) { ... }
     getProgress(syncId: string): SyncProgress | null { ... }
   }

   // SyncAllTransactionsUseCase で使用
   async syncInParallel(...) {
     for (let i = 0; i < targets.length; i += this.MAX_PARALLEL) {
       this.progressService.setProgress(syncHistory.id, {
         total: targets.length,
         completed: i,
         current: targets[i].institutionName,
       });
       // ... 同期処理
     }
   }
   ```

2. **Redis/Cacheを活用**
   - 分散環境でも動作するよう、Redisで進捗を管理

### 対応方針

- 別Issue/PRで実装予定

---

## 🟡 Medium: スケジュールAPI（モック実装）

### 概要

`SyncController.getSyncSchedule()`と`updateSyncSchedule()`がモック実装です。

### 現状

- 固定値を返すのみ
- `TODO`コメント: `// TODO: スケジュール管理の実装`

### Geminiレビュー指摘

> APIエンドポイントとしては存在しますが、実際のスケジュールの取得や更新は行われません。このままではAPI仕様と実装が乖離しています。

### 実装案

```typescript
// Controller
@Get('schedule')
async getSyncSchedule(): Promise<SyncScheduleResponseDto> {
  const schedule = await this.scheduledSyncJob.getSchedule();
  return {
    success: true,
    data: {
      enabled: schedule.enabled,
      cronExpression: schedule.cronExpression,
      timezone: schedule.timezone,
      nextRun: schedule.nextRun?.toISOString() || null,
    },
  };
}

@Put('schedule')
async updateSyncSchedule(@Body() dto: UpdateSyncScheduleRequestDto) {
  await this.scheduledSyncJob.updateSchedule(
    dto.cronExpression,
    dto.timezone,
  );
  await this.scheduledSyncJob.setEnabled(dto.enabled);
  return this.getSyncSchedule();
}
```

### 対応方針

- 動的スケジュール更新機能と合わせて実装
- または、一時的にエンドポイントをコメントアウト

---

## 📋 実装優先度と依存関係

### Phase 1: 基盤機能（現在のPR #278）

- ✅ ドメインモデル設計
- ✅ API設計
- ✅ 基本的な同期フロー
- ✅ 同期履歴の管理

### Phase 2: 金融機関連携（一部完了: Issue #279, PR #285）

- ✅ FR-002: クレジットカード連携（実装済み）
- ✅ FR-003: 証券会社連携（実装済み）
- 🔴 FR-001: 銀行口座連携（未実装）
- ✅ クレジットカード・証券のデータ取得ロジック実装
- ✅ 同期キャンセル処理実装（AbortController導入）

### Phase 3: 運用機能強化（別Issue）

- 🟡 動的スケジュール更新
- 🟡 リアルタイム進捗表示
- 🟡 スケジュールAPI実装
- 🟡 通知機能の実装
- 🟡 メトリクス収集の拡張

---

## 🔗 関連ドキュメント

- [詳細設計書: FR-006 README](./README.md)
- [シーケンス図](./sequence-diagrams.md)
- [API仕様](./input-output-design.md)
- [バッチ処理仕様](./batch-processing.md)

---

**最終更新**: 2025-11-24
**関連Issue**: #28, #279
**関連PR**: #278, #285
