# .env.example
# このファイルをコピーして .env を作成し、実際の値を設定してください
# cp .env.example .env

# ============================================
# Application Settings
# ============================================
NODE_ENV=development

# ============================================
# Backend Settings
# ============================================
PORT=3001

# ============================================
# Frontend Settings
# ============================================
NEXT_PUBLIC_API_URL=http://localhost:3001

# ============================================
# Docker Settings
# ============================================
COMPOSE_PROJECT_NAME=account-book

# ============================================
# Encryption Settings (Backend)
# ============================================
# 暗号化キー（32バイト以上のランダムな文字列）
# 以下のコマンドで生成できます:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=change-this-to-a-random-64-char-hex-string-for-production

# 暗号化ソルト（16バイト以上のランダムな文字列）
# 以下のコマンドで生成できます:
# node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
CRYPTO_SALT=change-this-to-a-random-32-char-hex-string-for-production

# ============================================
# Logging Settings (Backend)
# ============================================
LOG_LEVEL=info

# ============================================
# Future MySQL Settings (Issue #165)
# ============================================
# MySQL導入時に以下の設定を有効化してください
# MYSQL_HOST=mysql
# MYSQL_PORT=3306
# MYSQL_USER=account_book_user
# MYSQL_PASSWORD=your_secure_password
# MYSQL_DATABASE=account_book_dev
# MYSQL_ROOT_PASSWORD=your_root_password
